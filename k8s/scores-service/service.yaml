apiVersion: v1
kind: Service
metadata:
  name: scores-service
  labels:
    app: scores-service
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: scores-service
