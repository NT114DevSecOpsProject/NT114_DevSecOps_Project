apiVersion: v1
kind: Service
metadata:
  name: {{ include "scores-service.fullname" . }}
  labels:
    {{- include "scores-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scores-service.selectorLabels" . | nindent 4 }}
